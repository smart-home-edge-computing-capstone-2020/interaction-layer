# Use this instead of print so as to write to the log file instead of stdout
def log_error(err):

# Use this to log any occurrences in your program, such as an interaction
# happening, a config change, a heartbeat failing indicating a node went down,
# etc.
def log_action(action):
